# {{- if .Values.mlflow.enabled }}
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: {{ include "cnyrub-predictor.fullname" . }}-mlflow
#   labels:
#     {{- include "cnyrub-predictor.labels" . | nindent 4 }}
#     app.kubernetes.io/component: mlflow
# spec:
#   # SQLite backend не поддерживает несколько писателей одновременно -- для
#   # прод-нагрузки замените backend-store-uri на Postgres (env MLFLOW_BACKEND_STORE_URI)
#   # и artifact-root на S3-совместимое хранилище, тогда можно будет увеличить replicas.
#   replicas: 1
#   selector:
#     matchLabels:
#       app.kubernetes.io/name: {{ include "cnyrub-predictor.name" . }}-mlflow
#       app.kubernetes.io/instance: {{ .Release.Name }}
#   template:
#     metadata:
#       labels:
#         app.kubernetes.io/name: {{ include "cnyrub-predictor.name" . }}-mlflow
#         app.kubernetes.io/instance: {{ .Release.Name }}
#     spec:
#       containers:
#         - name: mlflow
#           image: "{{ .Values.mlflow.image }}"
#           args:
#             - mlflow
#             - server
#             - --backend-store-uri
#             - "{{ .Values.mlflow.backendStoreUri }}"
#             - --default-artifact-root
#             - "{{ .Values.mlflow.artifactRoot }}"
#             - --host
#             - "0.0.0.0"
#             - --port
#             - "{{ .Values.mlflow.port }}"
#           ports:
#             - name: http
#               containerPort: {{ .Values.mlflow.port }}
#           readinessProbe:
#             httpGet:
#               path: /
#               port: http
#             initialDelaySeconds: 10
#             periodSeconds: 10
#           livenessProbe:
#             httpGet:
#               path: /
#               port: http
#             initialDelaySeconds: 20
#             periodSeconds: 20
#           resources:
#             {{- toYaml .Values.mlflow.resources | nindent 12 }}
#           volumeMounts:
#             - name: data
#               mountPath: /mlflow
#       volumes:
#         - name: data
#           persistentVolumeClaim:
#             claimName: {{ include "cnyrub-predictor.fullname" . }}-mlflow
#       {{- with .Values.nodeSelector }}
#       nodeSelector:
#         {{- toYaml . | nindent 8 }}
#       {{- end }}
#       {{- with .Values.affinity }}
#       affinity:
#         {{- toYaml . | nindent 8 }}
#       {{- end }}
#       {{- with .Values.tolerations }}
#       tolerations:
#         {{- toYaml . | nindent 8 }}
#       {{- end }}
# {{- end }}
