apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnyrub-predictor.fullname" . }}
  labels:
    {{- include "cnyrub-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cnyrub-predictor.selectorLabels" . | nindent 4 }}
