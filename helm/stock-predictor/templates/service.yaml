apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-predictor.fullname" . }}
  labels:
    {{- include "stock-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stock-predictor.selectorLabels" . | nindent 4 }}
